import Contact from '@/Components/Contact/Contact';
import React from 'react';

export const metadata = {
    title: 'Bistro Boss | Contact Page',
    description: 'Best Food Restaurant',
}

const ContactPage = () => {
    return (
        <div>
            <Contact/>
        </div>
    );
};

export default ContactPage;