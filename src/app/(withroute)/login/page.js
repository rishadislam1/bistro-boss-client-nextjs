import Login from '@/Components/Login/Login';
import React from 'react';

export const metadata = {
    title: 'Bistro Boss | Login Page',
    description: 'Best Food Restaurant',
}

const LoginPage = () => {
    return (
        <div>
            <Login/>
        </div>
    );
};

export default LoginPage;